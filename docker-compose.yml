version: "3.9"

services:
  fb-ads-panel:
    build: .
    container_name: fb-ads-panel
    restart: unless-stopped
    ports:
      - "127.0.0.1:8811:8000"
    env_file:
      - .env
    volumes:
      - ./data:/app/data   # 持久化 SQLite 数据库（账户配置等）
